12 agents required. Launch one agent per block below, passing its block VERBATIM — copy, do not retype. The ───── lines are separators, not part of any prompt. This is the same roster `check-coverage` reads out of the plan: a block you skip or reword is a dimension nobody reviewed. Blocks are numbered `agent k of 12` and the output ends with an end-of-roster line — if either is missing, this output was truncated in transit: every prompt is also recorded on disk, so rebuild just the missing blocks with --chunk <id>, or --role <r> (--file <path> for an invariant agent), plus the same --rules this call was given.

The `description` parameter of each Agent call is the task name the user watches while the agent runs — write it in your output language, translating the block's ───── separator label (keep the role/chunk id visible). Display only: the prompt itself stays the block VERBATIM.

───── agent 1 of 12 — Agent 1a: Line-by-line correctness ─────

You are review agent `1a` — Agent 1a: Line-by-line correctness.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/1a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 2 of 12 — Agent 2: Security ─────

You are review agent `2` — Agent 2: Security.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/2.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 3 of 12 — Agent 3a: Reuse & duplication ─────

You are review agent `3a` — Agent 3a: Reuse & duplication.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/3a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 4 of 12 — Agent 3b: Altitude & abstraction fit ─────

You are review agent `3b` — Agent 3b: Altitude & abstraction fit.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/3b.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 5 of 12 — Agent 3c: Consistency & clarity ─────

You are review agent `3c` — Agent 3c: Consistency & clarity.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/3c.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 6 of 12 — Agent 4: Performance & efficiency ─────

You are review agent `4` — Agent 4: Performance & efficiency.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/4.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 7 of 12 — Agent 5: Test coverage ─────

You are review agent `5` — Agent 5: Test coverage.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/5.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 8 of 12 — Agent 6a: Undirected audit — attacker mindset ─────

You are review agent `6a` — Agent 6a: Undirected audit — attacker mindset.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/6a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 9 of 12 — Agent 6b: Undirected audit — 3 AM oncall mindset ─────

You are review agent `6b` — Agent 6b: Undirected audit — 3 AM oncall mindset.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/6b.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 10 of 12 — Agent 6c: Undirected audit — six-months-later maintainer ─────

You are review agent `6c` — Agent 6c: Undirected audit — six-months-later maintainer.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/6c.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 11 of 12 — Agent 1c: Cross-file tracer ─────

You are review agent `1c` — Agent 1c: Cross-file tracer.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/1c.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-diff.txt", offset=0, limit=319)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 12 of 12 — Agent 7: Build & test verification ─────

You are review agent `7` — Agent 7: Build & test verification.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/vm/Projects/personal/quantum-llm/.qwen/tmp/qwen-review-transformer_kernels.cu-plan-prompts/7.brief.md")
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── end of roster — 12 agents ─────
