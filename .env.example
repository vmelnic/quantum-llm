# Control-host SSH target used by ops/*.sh
QUANTUM_LLM_REMOTE=user@gpu-host
QUANTUM_LLM_REMOTE_ROOT=C:/quantum-llm

# Set on the Windows host before any non-loopback bind.
EXPERT_API_KEY=replace-with-a-secret
